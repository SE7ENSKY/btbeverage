extends root

block root
	- classes = []
	if headerMod
		- classes.push(headerMod)
	if headerDefault
		- classes.push('header_default')
	+b("header").header(class=classes): +i({
		menu: mainMenu
	})

	main.main#barba-wrapper
		.barba-container(data-namespace="page" data-header-classes=classes)
			block main
				!= content

	+b("footer").footer: +i

	+b.modals: +i({
		cartList: cartProducts
	})

	script(type="text/javascript" src="//s7.addthis.com/js/300/addthis_widget.js#pubid=se7ensky")

block fonts

block styles

block scripts

block analytics
