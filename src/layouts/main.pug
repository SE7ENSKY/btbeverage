extends root

block root
	- classes = []
	if headerMod
		- classes.push(headerMod)
	+b.header(class=classes): +i({ menu: mainMenu })

	main.main
		block main
			!= content

	+b.footer: +i

block fonts

block styles

block scripts

block analytics
